function makeNegative(num) {
  // conditional that returns number as is if greter than or equal to 0
  if (num <= 0) {
    return(num)
  // applies - and returns number if greater than 0
  } else {
    return(-num)
  }
}